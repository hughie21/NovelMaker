1.编辑器在粘贴大量文本时（字数超过10万）会造成卡顿，需要等待约5分钟才能加载完成。同时，大量文本下的编辑操作存在明显的卡顿。考虑更换为TinyMCE组件。

2.lang.json在编译过后的二进制文件中无法读取，考虑将其单独移出来。

3.在编辑器中粘贴文本时，如果文本中包含特殊字符，可能会导致导出的Epub文件存在异常，同时保存后的epmb文件在打开时，无法正确解析为json。

4.新增在保存后记录保存文件的路径，下次保存时直接根据路径而不是打开文件选择器。

